import type { NextConfig } from "next";

// Note: next-pwa has compatibility issues with Next.js 16 Turbopack
// For now, using standard config. Re-enable PWA when next-pwa updates.
// import withPWAInit from "next-pwa";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
